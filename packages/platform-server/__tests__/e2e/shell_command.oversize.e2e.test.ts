import { describe, it } from 'vitest';

// Legacy e2e for old ToolsNode behavior; kept as placeholder but skipped.
describe.skip('shell_command oversize output e2e (legacy - removed)', () => {
  it('skipped', () => {});
});
