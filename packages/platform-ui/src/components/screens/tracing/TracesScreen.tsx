import { TracingDisabledScreen } from './TracingDisabledScreen';

export function TracingTracesScreen() {
  return (
    <TracingDisabledScreen title="Tracing removed" message="Trace inspection is no longer available." />
  );
}
