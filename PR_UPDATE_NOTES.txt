Update: Removed Docker compose and Dockerfile for obs-server; server runs from sources via pnpm scripts. Added @agyn/obs-examples package with PoC script. Switched builds to esbuild + tsc for types; updated exports and start scripts.

Enhancements (Issue #99):
- Add registry-mirror service (registry:2 proxy) to docker-compose with shared network 'agents_net'.
- Extend ContainerService to support privileged, anonymous volumes, and createExtras for future-proof Docker options.
- Implement per-workspace DinD sidecar in ContainerProviderEntity.provide:
  - docker:27-dind, privileged, anonymous volume on /var/lib/docker
  - shares network namespace with workspace container (NetworkMode=container:<cid>)
  - inject DOCKER_HOST=tcp://localhost:2375 into workspace env
  - readiness check before initialScript execution
  - labels: hautech.ai/role=dind and hautech.ai/parent_cid=<workspaceId>
- Add DOCKER_MIRROR_URL config with default http://registry-mirror:5000; used in DinD --registry-mirror flag.
